language: bash

os:
 - linux

dist: xenial

addons:
 apt:
  packages:
   - make
   - gcc
   - fasm
   - git

before_script:
 - bash clean.sh

script:
 - bash rm -f bin/.gitkeep disk/system/kernel/.gitkeep
 - bash make.sh
 - bash upload.sh